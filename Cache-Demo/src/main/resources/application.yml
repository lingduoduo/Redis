spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:             # set if your Redis requires auth
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 32
          max-idle: 16
          min-idle: 4
          max-wait: 2000ms

server:
  port: 8080
